'use strict';

describe('clickApp.clickHome module', function() {

  beforeEach(module('clickApp.clickHome'));

  describe('clickHome controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ClickHomeCtrl');
      expect(ClickHomeCtrl).toBeDefined();
    }));

  });
});