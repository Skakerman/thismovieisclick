'use strict';

describe('clickApp.clickQuiz module', function() {

  beforeEach(module('clickApp.clickQuiz'));

  describe('clickQuiz controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var clickQuizCtrl = $controller('ClickQuizCtrl');
      expect(clickQuizCtrl).toBeDefined();
    }));

  });
});